{
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        /* Modules */
        "module": "esnext",
        "allowJs": true,
        "resolveJsonModule": true,

        /* Emit */
        "outDir": "backend",
        "rootDir": "./src",

        /* Completeness */
        "types": ["express-session"],
        "skipLibCheck": true,
    },
    "include": ["./src/backend"],
    "exclude": ["./src/frontend"],
    "lib": ["es2015"]
}
